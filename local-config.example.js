// Copy this file to local-config.js (same folder) and fill in your team's
// real TBA key. local-config.js is gitignored, so it is never committed —
// each deployment (or each mentor's laptop, if hosting locally) sets its
// own copy once, and every scouter's device then has TBA autofill working
// automatically with no per-device setup.
//
// Get a free read-only key at https://www.thebluealliance.com/account

window.DS_CONFIG = window.DS_CONFIG || {};
window.DS_CONFIG.tbaKey = 'YOUR_TBA_READ_API_KEY_HERE';
