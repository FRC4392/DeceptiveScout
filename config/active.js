// Which config folder is active. Point this at a different folder under
// config/ to switch seasons/games — nothing else needs to change.
//
// To add a new season: copy an existing config/<slug>/ folder, edit its
// six files for the new game, then change the slug below.

window.DS_ACTIVE_CONFIG = '2026-example';
